# 2026-06-27 — Django PR #21443: second review round + follow-up ticket

## Django PR #21443 (ogrinfo docs, Trac #25927)

- smithdc1 came back with a second, detailed review (6 points). Replied to each
  thread to confirm direction *before* coding, since one point was a structural
  change I didn't want to do twice.
- The 6 points and how I handled them:
  1. **Combine with ogrinspect** — agreed; the ogrinspect page was just two bare
     signatures anyway. Folded the ogrinfo docs into
     `docs/ref/contrib/gis/ogrinspect.txt`, deleted the standalone `ogrinfo.txt`,
     removed its toctree entry from `utils.txt`.
  2. **Link `DataSource` up top** — moved the `data_source` sentence into the
     opening paragraph so the link sits in the description.
  3. **Which CLI options match** — bare `ogrinfo` is a one-line summary; added a
     note that `-al` (all features) / `-so` (summary only) give comparable output.
  4. **Comma-heavy sentence** — split into two sentences.
  5. **Simpler wording** — took his suggestion ("in a similar format").
  6. **Document `num_features`** — added a dedicated sentence.
- Amended the single commit and force-pushed: `dc01bbf` → `2f2b971`. Nice side
  effect — vs the upstream parent the commit is now just AUTHORS +1 and
  ogrinspect.txt (ogrinfo.txt never gets created, utils.txt unchanged). No churn.
- Docs build clean with `sphinx -b html -W`.

## Follow-up: filed Trac #37193

- The `num_features` slicing quirk smithdc1 spotted: the code does
  `layer[:num_features]`, so `None` prints every feature and negatives drop from
  the end — neither matches what the arg name implies, and it's public API
  (`__all__`). Too much for a docs PR (needs validation + tests + maybe a
  deprecation path), so kept this PR docs-only and split it out.
- **Filed https://code.djangoproject.com/ticket/37193** — "ogrinfo() does not
  validate num_features, so None and negative values give surprising results"
  (Cleanup/optimization, GIS, dev). Triage stage Unreviewed, no patch yet.
- TODO: drop a reply on PR #21443 telling smithdc1 it's filed as #37193, to
  close the loop on his question (he hasn't confirmed the defer-to-ticket plan
  yet).
