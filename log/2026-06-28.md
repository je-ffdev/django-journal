# 2026-06-28 — Django #37193 resolved as a docs note in PR #21443

## Trac #37193 (ogrinfo num_features validation)

- smithdc1 triaged the ticket I filed: reformatted my description into Trac
  `{{{ }}}` code blocks (no content change), then in comment:2 proposed *not*
  adding validation + a deprecation path, but instead documenting the
  expectation as part of #25927 (i.e. on the existing PR #21443).
- Agreed — `ogrinfo()` is a developer debugging helper, documenting the contract
  is proportionate and avoids the backwards-compat risk of changing None/negative
  slicing behaviour. Folds into the open PR with no new patch churn.
- Drafted a reply to comment:2 agreeing + giving tightened docs wording. Posted
  it to the ticket myself (no Trac API from the tool side).

## PR #21443 docs edit

- Added to the `num_features` sentence in
  `docs/ref/contrib/gis/ogrinspect.txt`:
  > It is used as a list slice, so it should be a non-negative integer.
- Tweaks vs smithdc1's suggested text: "displayed per layer" not "returned"
  (it prints, doesn't return), and prescriptive docs voice instead of "typically
  expected to be".
- Docs build clean with `sphinx -b html -W`.
- Amended the single commit + force-pushed: `2f2b971` → `dcf5813`. Net diff vs
  `upstream/main` still just AUTHORS +1 and ogrinspect.txt — no churn.
- Posted the agree-and-document reply to comment:2 on #37193, matching smithdc1's
  own format (short line + `>` blockquote with the proposed sentence in single
  backticks) rather than `{{{ }}}`.

## Resolution / how it landed

- Reply on #37193 is now in smithdc1's hands — proposed the exact sentence that's
  committed, framed as "slightly tightened for the docs voice," so he can react to
  the precise wording.
- Did **not** use his suggested text verbatim, and that's deliberate: his
  "What do you think about..." was an invitation, not a directive, and the
  suggest→adapt loop is how this whole PR has gone. More importantly, his
  "limit the number of features returned" was a small inaccuracy — `ogrinfo()`
  *prints*, it returns nothing — so verbatim would have baked that into the docs.
  Kept the correctness fix and flagged the change transparently instead of
  re-commenting to over-explain a routine adaptation.
- State: PR #21443 force-pushed (`dcf5813`), reply posted, ball is with smithdc1.
  #37193 stays open as a docs-only follow-up that closes when #25927 lands.
