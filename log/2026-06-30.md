# 2026-06-30 — Trac #37193 closed wontfix; docs-in-#25927 approach wins

## Trac #37193 (ogrinfo num_features validation) — CLOSED

- smithdc1 closed the ticket I filed as **wontfix** (comment:5): "I'll close
  this ticket to try and avoid folk working on something you've already well
  progressed. I'll leave a note on the other ticket back to this as well."
- This ratifies the defer-to-docs plan from #37193 comment:2/comment:3 — no
  code-level validation or deprecation path; the `num_features` contract is
  documented instead, in PR #21443 (#25927). The backwards-compat risk of
  changing None/negative slicing is avoided.
- maheen8q (comment:4, ~24h before close) had offered to add validation
  (ValueError for negatives, TypeError for non-ints, None left to reviewer).
  smithdc1's close pre-empts that to avoid duplicate/divergent work, pointing
  back to my already-progressed documentation.

## #25927 cross-link confirmed (comment:8)

- smithdc1 posted the promised back-reference on #25927 (comment:8, same ~112min
  window as the #37193 close): "we discussed that num_features is used as a list
  slice without any validation... we'd document that behaviour as part of this
  ticket." Both tickets are now mutually consistent — #37193 points to the docs,
  #25927 records why the docs cover it.

## ACTION NEEDED: uncheck "Patch needs improvement" on #25927

- Ticket header still shows **Patch needs improvement: yes** — set by smithdc1 in
  comment:7 during the 2nd review round. I addressed all 6 points 2026-06-27 and
  posted replies, but never unchecked the flag.
- In Django's workflow this is the *contributor's* signal that review is
  addressed and the patch is ready for another pass. While it stays set, the PR
  doesn't appear in the "ready for review" queue and can stall. smithdc1
  commenting (comment:8) without flipping it himself points to him waiting on me.
- TODO: uncheck "Patch needs improvement" on #25927 (no Trac API from tool side —
  must do it in the browser), optionally with a one-line comment that the 2nd
  review round is addressed in `dcf5813`.

## DONE: flag flipped (comment:9)

- Unchecked "Patch needs improvement" on #25927 (comment:9). Header now reads
  **Patch needs improvement: no**; next-step box flipped to "for anyone except
  the patch author to review... mark Ready for checkin."
- Flipped the flag only — did not post the drafted recap comment. Fine: the flag
  is what routes the ticket back into the review queue. Recap was optional.

## State

- Nothing actionable left on #37193 — closed wontfix, cross-link posted.
- #25927 / PR #21443: docs committed (`dcf5813`), replies posted, "Patch needs
  improvement" now UNSET. Genuinely in reviewers' hands — next move is a
  reviewer marking Ready for checkin or leaving new comments. Nothing in my
  court until then.
